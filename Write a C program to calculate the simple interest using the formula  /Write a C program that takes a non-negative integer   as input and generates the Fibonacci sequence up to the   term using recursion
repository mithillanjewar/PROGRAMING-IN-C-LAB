
3.1.3. Fibonacci Series using Recursion
18:10
Write a C program that takes a non-negative integer 
 as input and generates the Fibonacci sequence up to the 
 term using recursion.



Input Format:

A single non-negative integer 
.


Output Format:

Print the first 
 terms of the Fibonacci sequence, separated by spaces.


#include <stdio.h>

// Recursive function to print Fibonacci numbers
void printFibonacci(int n, int t1, int t2) {
	if(n>0)
	// Write your code...
	{printf("%d ", t1);

	printFibonacci(n - 1, t2, t1 + t2 );
	}
}

int main() {
    int n;

    // Input: Read the number of terms
    scanf("%d", &n);

    // Start the recursion with the first two Fibonacci numbers (0 and 1)
    printFibonacci(n, 0, 1);

    return 0;
}
Reason for late submission
Type reason here
Please enter at least 15 characters

